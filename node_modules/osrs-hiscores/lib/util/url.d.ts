import { SkillName, ActivityName } from '../types';
/**
 * Builds the url string for personal stats
 *
 * @param {string} player The player name to lookup
 * @param {Mode} [mode] The game mode to select. Defaults to `normal`
 *
 * @returns {string}
 */
export declare const buildStatsUrl: (player: string, mode?: "normal" | "ironman" | "ultimate" | "hardcore" | "deadman" | "seasonal" | "tournament" | undefined) => string;
/**
 * Builds the url string for skill page
 *
 * @param {SkillName} skill The skill name to lookup
 * @param {Mode} [mode] The mode to select. Defaults to `normal`
 *
 * @returns {string}
 */
export declare const buildSkillPageUrl: (skill: SkillName, mode?: "normal" | "ironman" | "ultimate" | "hardcore" | "deadman" | "seasonal" | "tournament" | undefined) => string;
/**
 * Builds the url string for activity page
 *
 * @param {ActivityName} activity The activity name to lookup
 * @param {Mode} [mode] The mode to select. Defaults to `normal`
 *
 * @returns {string}
 */
export declare const buildActivityPageUrl: (activity: ActivityName, mode?: "normal" | "ironman" | "ultimate" | "hardcore" | "deadman" | "seasonal" | "tournament" | undefined) => string;
/**
 * Builds the url string for formatted player name (highlighted in table)
 *
 * @param {string} player The player to lookup
 * @param {SkillName} [skill] The skill table to use. Defaults to `overall`
 * @param {Mode} [mode] The mode to select. Defaults to `normal`
 *
 * @returns {string}
 */
export declare const buildSkillPageWithDisplayNameUrl: (player: string, skill?: "hunter" | "overall" | "attack" | "defence" | "strength" | "hitpoints" | "ranged" | "prayer" | "magic" | "cooking" | "woodcutting" | "fletching" | "fishing" | "firemaking" | "crafting" | "smithing" | "mining" | "herblore" | "agility" | "thieving" | "slayer" | "farming" | "runecraft" | "construction" | undefined, mode?: "normal" | "ironman" | "ultimate" | "hardcore" | "deadman" | "seasonal" | "tournament" | undefined) => string;
